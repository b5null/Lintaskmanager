cmake_minimum_required(VERSION 3.16)
project(lintaskmanager LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
find_package(Qt6 REQUIRED COMPONENTS Widgets)
qt_add_resources(ICONS installer/resources.qrc)
add_executable(lintaskmanager src/task-manager-qt.cpp ${ICONS})
target_link_libraries(lintaskmanager PRIVATE Qt6::Widgets)
